server:
  port: 5273

spring:
  application:
    name: DCForge

  datasource:
    url: jdbc:mysql://localhost:3306/dc_forge?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: Dingtong1998
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true # 开启驼峰映射

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false

minio:
  cleanup:
    cron: "0 0 2 * * ?"             # 每天凌晨2点（生产环境）

claude:
  base-path: "C:\\Users\\22390\\.claude"
